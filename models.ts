import path from "path";

export interface ISoul {
    name: string,
    address: string,
    img?: any,
    totalAum?: number,
    members?: number,
    diversity?: number,
    cases?: number,
    positive?: number,
    negative?: number,
    upvotes?: number,
    downvotes?: number
}